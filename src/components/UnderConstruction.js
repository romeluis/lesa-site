const UnderConstruction = () => {
    return ( 
        <div className="stripped-yellow" style={{borderRadius: "1rem", padding: "1rem 1.5rem 0.8rem 1.5rem", display: "flex", justifyContent: "center"}}>
            <h1 className="construction-text" style={{borderRadius: "1rem", padding: "1rem 1.5rem 0.8rem 1.5rem", backgroundColor: "var(--white)", fontFamily: "object_sans_bold"}}>This page is not ready yet. Check back soon.</h1>
        </div>
     );
}
 
export default UnderConstruction;